<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//KO"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hana.app.repository.DepogKeywordRepository">

    <delete id="reset">
        TRUNCATE depositKeyword
    </delete>

    <insert id="insert" parameterType="depositKeywordDto">
        INSERT INTO savingKeyword VALUES (#{productId},#{termId},#{termClass},#{termContent},#{termRate})
    </insert>



</mapper>